package plain_java;

/**
 * @author jeffreymeyerson
 *
 * The Function Class.  Functions are used by SongDataManagers to produce outputs
 * from generic parameter types that are consistent across time.
 *
 */
public class Function {
		
	/**
	*
	* A generic uptrend with an upper bound. x has a stronger impact on the
	* output than y.  This function could be applied by a SongDataManager
	* that is trying to produce price that is positively correlated with high
	* frequency (x), somewhat positively correlated with mid frequency (y), and 
	* negatively correlated with low frequency.
	*
	**/
//	public Function uptrendWithUpperBound(double x, double y, double z, int upperBound){
//		double result = ( 2 * x + y ) / z;
//		result < upperBound ? return result: return upperBound;
//	}
    
}
